Drop NPC/monster JSON files here (same schema as `Character Sheets/*.json`,
one file per NPC) to have them built into real pages on the site — visible
only in DM mode, never listed for players.

See `../build/NPC_TEMPLATE.md` for the JSON shape to hand Claude when you
want one generated, and `../build/README.md` for the full build/deploy loop.
